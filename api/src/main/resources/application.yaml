spring:
  profiles:
    include:
      - domain
      - common
  main:
    banner-mode: off
---
spring:
  config:
    activate:
      on-profile: local
---
spring:
  config:
    activate:
      on-profile: dev
---
spring:
  config:
    activate:
      on-profile: prod
